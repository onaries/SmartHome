<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <PreferenceCategory android:title="@string/pref_header_general"/>
    <EditTextPreference
        android:key="server_ip"
        android:title="@string/pref_serverip"
        android:defaultValue="@string/pref_defaultip"
        android:selectAllOnFocus="true"
        android:dialogMessage="@string/pref_server_no_protocol"
        android:singleLine="true"
        android:maxLines="1" />

    <EditTextPreference
        android:key="server_port"
        android:title="@string/pref_server_port"
        android:defaultValue="@string/pref_default_port"
        android:selectAllOnFocus="true"
        android:capitalize="words"
        android:singleLine="true"
        android:maxLines="1" />
    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:key="server_time"
        android:title="@string/pref_server_time"
        android:defaultValue="@string/pref_default_server_time"
        android:selectAllOnFocus="true"
        android:numeric="integer"
        android:inputType="number"
        android:singleLine="true"
        android:maxLines="1" />

</PreferenceScreen>
